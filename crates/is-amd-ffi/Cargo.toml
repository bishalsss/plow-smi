[package]
name = "is-amd-ffi"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
description = "Pure-Rust AMD SMI via dlopen — shared library for AMD GPU telemetry and control"

[features]
default = []
# When enabled on x86_64, resolves libamd_smi.so at runtime.
# Otherwise stub implementations are provided.
amd = []

[dependencies]
libloading = "0.8"
